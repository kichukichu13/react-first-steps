import React from 'react';
import PropTypes from 'prop-types';

import './header.css';

const Header = () => (
    <header></header>
);

Header.propTypes = {};

Header.defaultProps = {};

export default Header;
